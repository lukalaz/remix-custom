generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["referentialIntegrity"]
}

datasource db {
  provider             = "sqlserver"
  url = "sqlserver://lukalaz-server-db.database.windows.net:1433;database=remix-blog-database;user=lukalaz-user;password=tP6j0X2r85W@;encrypt=true"
  referentialIntegrity = "prisma"
}

model Post {
  slug            String   @id
  title           String
  markdown        String
  excerpt         String
  seo_title       String
  seo_description String
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}

model Project {
  slug            String   @id
  title           String
  markdown        String
  excerpt         String
  seo_title       String
  seo_description String
  technologies    String
  github          String
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
